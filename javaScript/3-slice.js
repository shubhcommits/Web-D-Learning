var tweet=prompt("compose your tweet");
alert(tweet.slice(0,140));  // here slice is used to  cut some part of paragraph    
// upper both lines can be written in a single line as 
alert(prompt("Compose your tweet").slice(0,140))