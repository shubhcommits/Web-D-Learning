// var nam="shubham";
// nam.toUpperCase();
// nam.toLowerCase();
/******************************************************** */
var nam=prompt("Enter your name")
alert(nam.slice(0,1).toUpperCase()+nam.slice(1,nam.length).toLowerCase()); // this will only chage case of first letter