// to cound how many characters are written and how mant many characters are left
/*var totalLength = 180; 
var character = prompt("Enter the characters");
var len = character.length;
var left = totalLength-len;
alert("You have written "+len+" characters, you have "+left+" characters left.")*/
var character = prompt("Enter the characters");
alert("You have written "+character.length+" characters, you have "+(180-len)+" characters left.")