let marks = [98,95,99,96,90]
marks[marks.length]= 100 // adding new element to array
marks[3]=97  // changing value in array
for(i=0;i<marks.length;i++){
  console.log(marks[i])
} 
// arrays are mutable they can be changed while
// strings are immutable they can not be changed
// array is an object
